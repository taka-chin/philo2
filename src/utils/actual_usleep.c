
#include "philo.h"

void actual_usleep(long int milisec)
{
	/* long int		start_time; */

	/* start_time = get_now_time(); */
	/* while(get_now_time() - start_time < sleep_time) */
					/* ; */
	usleep(milisec * 1000);
}
